    #other ways to write the same thing
    # if not label in label_freq: dc[label] = 0.0
    # label_freq[label] += 1
    # if not label in label_freq: dc[label] = 1.0
    # else: label_freq[label] += 1

    # if not word in spam_freq: spam_freq[word] = 1.0
    # spam_freq[word] += 1
    
    # if not word in ham_freq: ham_freq[word] = 1.0
    # ham_freq[word] += 1
